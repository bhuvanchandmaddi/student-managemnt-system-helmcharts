spring:
  datasource:
    url: jdbc:mysql://mysql:3306/studentsms
    username: springboot
    password: springboot
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG

greeting:
  message: ${GREETINGS}
